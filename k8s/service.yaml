apiVersion: v1
kind: Service
metadata:
  name: oficina-api-service
  labels:
    app: oficina-api
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  type: LoadBalancer
  selector:
    app: oficina-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
