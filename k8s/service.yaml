apiVersion: v1
kind: Service
metadata:
  name: oficina-api-service
  labels:
    app: oficina-api
spec:
  type: ClusterIP
  selector:
    app: oficina-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
